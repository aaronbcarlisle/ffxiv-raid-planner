import '@testing-library/jest-dom/vitest';
// Initialize i18next so components using useTranslation render real strings
// (English via fallbackLng) instead of raw keys during tests.
import '../i18n';
