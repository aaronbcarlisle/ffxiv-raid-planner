/**
 * Width of the static Settings panel. Single-sourced so the dock width and the
 * header's right-padding can't drift. Matches the SettingsPanel `SlideOutPanel`
 * `width="3xl"` (max-w-3xl = 48rem) so docking doesn't reflow settings content.
 */
export const SETTINGS_PANEL_WIDTH = '48rem';
